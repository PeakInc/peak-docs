---
title: "Quick Start"
description: "Run a GitHub Actions job on Peak in 10 minutes."
---

## Before you start

- Sign up at [peak.inc](https://peak.inc).
- Be an owner of your GitHub organization.
- Connect your GitHub organization in Settings.

## Terminal setup

Run this in your repository folder.

```bash
npx @peak-inc/init
```

## Coding agent setup

Paste this into your coding agent.

```text
Set up Peak in this repository. Run npx -y @peak-inc/init@latest --json and follow each step.
```

## Peak agent setup

Run this in your repository folder.

```bash
npx -y @peak-inc/wizard@latest
```

## Manual setup

Change the runner in your workflow file.

```yaml .github/workflows/ci.yml
runs-on: ubuntu-latest # [!code --]
runs-on: peak-ubuntu-24.04-4 # [!code ++]
```
